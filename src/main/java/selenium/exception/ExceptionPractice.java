package selenium.exception;

public class ExceptionPractice {

    // No such element exception -> when any given locators like by id, name, xpath, css selectors do not point
    // to any particular web element in the dom (document object model) source view in html

    // timeout exception -> when web is loading and implicit or explicit wait passed

    //NoSuchWindow Exception -> when trying to switch to another doesn't exist

    //StaleElement Exception -> when we successfully located element but
}
